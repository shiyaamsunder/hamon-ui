import { Meta, ArgsTable, Story, Canvas } from "@storybook/addon-docs";
import { Badge } from "./badge";

<Meta title="Docs/Badge" component={Badge} />

export const Template = (args) => <Badge {...args} />;

# Badge

Lets define a story for our **`<Badge/>`** component:

<Story name="My Badge" args={{ children: "Hello" }}>
  {Template.bind({})}
</Story>
